import styled from 'styled-components';

export const MenuAppStyle = styled.div`

/* general */
.holder{
    position:relative;
    top:0px;
    z-index: 10;
    .overlay{
        position:fixed;
        top:0px;
        left:0px;
        height:100vh;
        width:100vw;
        background-color:rgba(0,0,0,.2);
    }
    .menu{
        .button{

        }
    }
}


/** Device Styles */

  /* mobile styles */
  @media only screen and (max-width: 720px) {
  
  }

`;
